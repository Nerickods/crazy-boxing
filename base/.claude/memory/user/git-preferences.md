# Preferencias de Git

- **Regla de Oro**: NO subir cambios al repositorio remoto (`git push`) de forma automática.
- **Protocolo**: El agente debe esperar a una petición explícita del usuario ("subelo", "haz push", "actualiza el repo") antes de ejecutar comandos de push.
- **Fecha de registro**: 2026-04-02
